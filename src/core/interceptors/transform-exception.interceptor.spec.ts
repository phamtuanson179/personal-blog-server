import { TransformExceptionInterceptor } from './transform-exception.interceptor';

describe('TransformExceptionInterceptor', () => {
  it('should be defined', () => {
    expect(new TransformExceptionInterceptor()).toBeDefined();
  });
});
